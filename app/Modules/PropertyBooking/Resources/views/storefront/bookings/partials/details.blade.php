<div class="pb-stay-public-booking-grid"><section class="pb-stay-public-panel" aria-labelledby="booking-stays-title"><div class="pb-stay-public-panel__heading"><div><p class="pb-storefront-eyebrow">Accommodation</p><h2 id="booking-stays-title">Reserved stay</h2></div><span>{{ $booking->stays->count() }} {{ \Illuminate\Support\Str::plural('space', $booking->stays->count()) }}</span></div><div class="pb-stay-public-lines">@foreach($booking->stays as $stay)<article><div><p>{{ $stay->rate_plan_name }}</p><h3>{{ $stay->unit_type_name }}</h3><span>{{ $stay->billable_units }} {{ \Illuminate\Support\Str::plural($stay->pricing_unit->label(), $stay->billable_units) }} for {{ $stay->adult_count + $stay->child_count }} guests</span></div><strong>{{ \App\Modules\PropertyBooking\Support\MoneyFormatter::format($stay->total_minor, $booking->currency) }}</strong></article>@endforeach</div>@if($booking->special_requests)<div class="pb-stay-public-request"><strong>Special request</strong><p>{{ $booking->special_requests }}</p></div>@endif</section><aside class="pb-stay-summary"><p class="pb-storefront-eyebrow">Summary</p><h2>{{ $booking->booking_number }}</h2><dl><div><dt>Accommodation</dt><dd>{{ \App\Modules\PropertyBooking\Support\MoneyFormatter::format($booking->accommodation_subtotal_minor, $booking->currency) }}</dd></div><div><dt>Tax {{ $booking->tax_inclusive ? '(included)' : '' }}</dt><dd>{{ \App\Modules\PropertyBooking\Support\MoneyFormatter::format($booking->tax_minor, $booking->currency) }}</dd></div><div class="pb-stay-summary__total"><dt>Total</dt><dd>{{ \App\Modules\PropertyBooking\Support\MoneyFormatter::format($booking->total_minor, $booking->currency) }}</dd></div><div><dt>Paid</dt><dd>{{ \App\Modules\PropertyBooking\Support\MoneyFormatter::format($booking->paid_minor, $booking->currency) }}</dd></div><div><dt>Balance</dt><dd>{{ \App\Modules\PropertyBooking\Support\MoneyFormatter::format($booking->balance_minor, $booking->currency) }}</dd></div></dl><div class="pb-stay-public-meta"><span><i data-lucide="calendar-clock" aria-hidden="true"></i>{{ $booking->stay_status->label() }}</span><span><i data-lucide="wallet-cards" aria-hidden="true"></i>{{ $booking->preferred_payment_method?->label() ?? 'To be confirmed' }}</span><span><i data-lucide="badge-check" aria-hidden="true"></i>{{ $booking->payment_status->label() }}</span></div></aside></div>
